services:
  app:
    image: louislam/uptime-kuma:1.23.13-debian
    networks:
      - public
    env_file:
      - ./app.env
    volumes:
      - data:/app/data

networks:
  public:
    external: true

volumes:
  data:

